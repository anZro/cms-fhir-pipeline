name: 'dbt_fhir'
version: '1.0.0'
config-version: 2

profile: 'dbt_fhir'

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  dbt_fhir:
    staging:
      +materialized: view
      +schema: bcda_fhir
    intermediate:
      +materialized: view
      +schema: bcda_fhir
    marts:
      +materialized: table
      +schema: bcda_fhir
